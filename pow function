Add library (math.h) Pow function which uses  a power of numbers. 
firstly  include library (math.h) Pow function use  a power of numbers. 
//-----this is code

#include <stdio.h>
#include<math.h>

int main()
{
   printf("%f",pow(5,3));
    

    return 0;
}
